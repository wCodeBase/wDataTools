import { Box } from 'ink';
import React from 'react';

export const HorizontalSelect = <T extends unknown>(props:{options: {label: string, value:T}[],onSelect: (value: string)=>void})=>{
    // TODO: HorizontalSelect
    return <Box>
        
    </Box>
}