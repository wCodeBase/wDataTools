{
  "name": "root",
  "private": true,
  "devDependencies": {
    "husky": "4",
    "lerna": "^4.0.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lerna run --concurrency 1 --stream lint-staged --since HEAD --exclude-dependents"
    }
  }
}
